name: Test ARC Runner Scale Set
on:
  workflow_dispatch:

jobs:
  test-arc:
    runs-on: arc-runner-set
    steps:
    - run: echo "🎉 This job uses runner scale set runners!"
    - run: uname -a
    - run: kubectl version --client || echo "kubectl not found"